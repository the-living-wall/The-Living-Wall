# 正式开启前的具体决策

Refs #88。不是已执行授权。无需 OPA 或新购资源。

## A. 正式来源与日志

拟仅在现有环境 env-d1g2bv5sn355fc36e / ap-shanghai 增加 thelivingwall.cn 安全来源，保持具名 xiaoying-gifts-sdk-probe 调用，不添加通配符、不开放数据库/清理函数/匿名存储写入。

拟只将该 SDK 函数 IgnoreSysLog 从 false 调整为 true，并部署已测试的诊断入口：仅固定事件名、状态码、耗时。影响是该函数系统响应/开始结束等日志不再上报，保留应用诊断与平台指标；不承诺仅过滤正文。需实际调用及日志回读确认正文不再记录且诊断可用；不删除历史日志，不修改其他函数。

回退：移除本轮新增正式来源，恢复此前函数部署/配置；不恢复或删除数据库内容。任何实际日志验证失败，维持正式功能关闭。

## B. 费用与首批容量

现有个人版自动续费和超额使用均开启。建议首批小范围使用时保持原套餐和自动续费，但关闭超额按量使用；已有套餐配额用尽时可能停止服务，需要接受此可用性取舍。该开关不保证历史费用、其他产品或续费变成零费用。

本次保留已验证的全站 120 请求/分钟上限，前台每页约 20 次/分钟读取；可用于少量双方会话联调，不能称为广泛公开容量。若目标是同时多人公开使用，应先确定预算和人数，再调整轮询/入口配额并做对应容量验收；不把六页读满阈值的方案说成无限可用。

## C. 保存与删除承诺

现有活动库从创建起七天到期不可访问，定时清理；用户主动删除也仅能直接验证活动库。账号已有备份回档点；官方文档称默认备份保留七天，未取得本账号所有物理副本的独立到期证明。

需要用户决定是否接受并对用户说明：“交流创建七天后不可访问；活动数据会清理。云平台备份仍按平台保留周期清除，删除不意味着所有副本立即物理消失。” 不将其偷换成“创建后第七天所有副本消失”。若要求后者，需要先落实平台可验证的备份策略，不直接开放真人交流。

AGENTS.md 要求：涉及权限扩大、费用或对外承诺的变更，须由人类确认具体影响。因此 A/B/C 在执行前集中确认；这不是要求重新确认已经通过的人工体验测试。确认后仍须完成最终 HTTPS 来源首访、响应头、冷启动恢复、依赖合并及实际部署验收；确认本身不等于生产已发布。
