# 分享引导第二版 · 2026-09-23

Refs #88 / Draft PR #91。延续原测试站，不替换生产网站。

## 用户真机反馈

用户在电脑创建、手机打开邀请后反馈“我测试通过”。记为电脑与手机基本互发的人类反馈；不推定共同塑造、声音、视觉连续性、函数冷启动或实例切换均已通过。此前用户误把首页/地址栏网址当邀请，分享入口隐藏在折叠区，本轮针对该困惑修改。

## 界面变更

- 创建后直接显示“复制邀请链接”，说明仍需通过聊天发给一位朋友。
- 复制成功显示粘贴指引；失败不显示成功，展开并选中完整链接供手动复制。
- 仅实际收到接收者留言时显示“朋友已回复”；没有已读状态或催促提示。
- 默认域名的首页预先确认流程不变，未实现首次邀请直达。

## 验证

- 95/95 测试、类型检查、最终 lint、构建与 9 项规格校验通过。首次 lint 因状态提示使用的无障碍标签不符规则失败，保留日志；修正为 output 后重验。
- 两轮本地三场景通过（最终版本 share-v2-final）：真实复制并读取剪贴板、无展开即可发现复制按钮、模拟剪贴板拒绝并验证完整链接回退、实际回复状态、刷新、本地服务重启、断网重试、共同确认及删除。
- 桌面 1280×850 与窄屏 390×844 截图，窄屏无横向溢出；已目视检查分享区和失败回退。手动复制截图中邀请字段已遮罩；完整浏览器追踪仅保存在本机 ignored outputs，不提交凭证。
- results.json 的 source 是验证时父提交，workingTree 明确包含本轮未提交修改，不能解读为父提交已经具备新界面。

## 部署与剩余事项

静态页面源 `92578e62c4e4260a779b978e332a88478fe0072a` 已上传独立测试环境，20 个文件上传成功；未启用 CLI --verify，不记作散列远端验证通过。真实云端页面可见新版分享界面。API 仍为 `7e30d0b`，数据库、期限规则及域名均不改变。产物散列与环境见 deployment.json，上传记录见 upload.log。当前线上 index.html 已下载至 outputs/gifts/rollback-share-v2/index.html；旧散列资源保留，回退可恢复该入口（index-MhtZs_-e.js / index-ubcfabZI.css），无需恢复任何留言数据。

子域名只读检查：CloudBase 托管 Online，仍是平台默认域名；控制台浏览器连接超时，之前 DNS 管理 API 未授权，未重试绕过、未改 DNS 或购买资源。独立子域名、完整静态安全头、备份清理边界和实例切换继续待办。PR 保持 Draft。

## 云端发布后回归

第一轮安静陪伴全部通过；轻松打趣已完成复制、回复与共同确认，在删除后读取阶段平台返回 429，页面显示“暂时无法保存”，与预期删除提示不符，保留失败报告及截图。没有页面脚本异常；不将该轮写为三场景通过。后续在独立场景之间等待 45 秒，降低自动化请求集中程度，不改网关/服务限流，不放宽断言。限流错误的友好提示作为后续改进，当前不以泛化报错推断数据保存失败。

第二轮 cloud-paced：安静陪伴、轻松打趣完整通过；共同约定在腾讯云首次确认后的 DOM 加载等待超时，尚未创建该场景的心意。失败截图已显示小莹首页（无脚本异常），说明应用已出现而浏览器文档事件未在 12 秒内满足。复现脚本改为等待可见应用根节点，不降低后续业务断言；新增 QA_SCENARIO_INDEX=2 可只重跑共同约定，避免重复请求。原两轮失败均保留。

复现：使用首次部署记录的远程命令，设置唯一 QA_OUTPUT 目录；完整回归省略 QA_SCENARIO_INDEX，单独共同约定指定 QA_SCENARIO_INDEX=2。云端场景之间自动间隔 45 秒，前端轮询和真实操作照常进行。

第三轮 cloud-agreement：仅补测共同约定，复制、邀请、互发、同版本双方确认、仅纪念且保持原形态、删除后失效全部通过。三种场景的最终通过来自 cloud-paced 的前两项与 cloud-agreement 的一项；不是单轮三项全绿。所有测试只操作虚构记录，成功流程均由发送者删除，未操作用户正在使用的心意。后台没有重新部署，生产站保持原状。
