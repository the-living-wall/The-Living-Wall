# 朋友互动体验收口 · Refs #95 / #88

## 已有生产验收

2026-09-26 用户在正式网址确认「收发、共同确认、刷新均通过」。EdgeOne 部署 `dpuoitv6jpf0`，main 提交 `4c819199b2f931f27a32e941cf247a1275299a24`。这是用户真机反馈，不是本轮自动化测试。保留此前已通过的生产结果。

本轮修复分支从 `d95cdd1` 派生；应用基线仍为上述已部署提交，新增主线提交仅为项目动态。用户还报告 Android 微信首页朋友入口点不动、声音提示与关闭按钮矛盾，以及朋友页面文案和位置问题。

## 原因与改动

- `.bottom` 禁止空白区域捕获指针，朋友链接未重新开启 `pointer-events`；手机声音控件绝对定位又覆盖入口。恢复链接命中及至少 44px 点击高度，声音进入正常流。
- 音效/音乐任意一个失败都错误提示音效未启动。统一首页与朋友页组件，分别判定两路状态；任一路成功即可关闭，部分失败可明确重试。返回页面尊重关闭意图。
- 生成与分享移到左侧业务区；只保留一处分享入口，复制失败仍可选择完整链接。双区持续显示，最近三条留言、历史展开、待确认提议不隐藏。
- 引导 2 秒后约 300ms 淡出，悬停/焦点暂停，玩法说明可重读；弱动画关闭淡出。只新增 `xiaoying-friends-guide-v1`。
- 保存详情折叠，保存期限保留右下；创建未确认只显示一条提示，重试仍沿用原请求。

## 首次创建超时：限定只读诊断

用户最初看到了前端构造的 504 提示，随后在原页面重试生成成功，之后完成正式收发与共同确认。源码中 12000ms 计时覆盖 SDK 加载、匿名登录、函数调用及结果处理；SDK 本身也配置 12000ms。截图没有阶段时间、平台请求 ID 或脱敏失败日志，无法归因于网络、冷启动、登录、权限或数据库。

本轮核对既有记录与上述调用链后停止推测，没有扩大权限、延长阈值、自动重试或增加云资源。根因仍未确认；若再现，采集阶段及状态/耗时即可，不收集凭据或留言。独立本地故障注入验证“已保存但响应丢失”后重试请求相同，说明恢复机制可用，不证明首次生产超时已解决。

## 验证记录

- `npm test`：108/108；typecheck、lint、build、build:tencent 通过。Tencent 构建用 CI 假环境参数核对产物，不连接生产。
- 第一轮测试在沙箱内因本地监听 `EPERM` 失败；允许本地测试服务后通过，未删测试。
- 首轮引导 lint 指出 effect 同步更新和非交互元素 tabIndex，修正为挂载计时器、玩法说明入口承接焦点暂停。
- 浏览器脚本第一轮测试壳缺少 React preamble，第二轮使用了错误的接受按钮文案；修正测试壳与实际按钮名后继续验证，未改验收要求。
- 浏览器证据与最终检查结果见下方后续记录。浏览器使用隔离的本地 SQLite 数据及虚构留言，不访问用户生产交流；原始 trace/邀请凭据不提交。

## 发布边界

当前为候选实现，未合并、未发布。先完成桌面集中预览与 Android 微信人工验收；通过后合并 main，沿用原 EdgeOne 构建/环境变量部署新提交。生产回退部署为 `dpuoitv6jpf0`。本轮没有云权限、接口、数据模型、资源或费用变化。

## 本轮完成证据

- 新浏览器脚本 `scripts/friend-polish-browser-qa.mjs`：音效/音乐四种成功失败组合、部分失败重试、关闭、隐藏/返回与关闭意图保持；390/1280 首页真实链接命中与跳转；保存后丢响应原请求重试；复制失败全选回退；最近三条与展开历史接收新消息；引导计时、焦点/悬停暂停、键盘说明、仅一次、存储失败和弱动画。均通过。
- 原双端脚本 `QA_SCENARIO_INDEX=0 node prototypes/live-gift/qa-cross-device.mjs`：安静陪伴→沉静，通过断网草稿、创建刷新、服务重启、复制及失败回退、共同确认、浏览器刷新、真实音频资源启停/快速移动试听、删除与期限定位，无页面错误。仅更新与已批准文案/位置变化相关的断言，没有删业务验收。
- 严格规格校验 11/11；已有 local-sound-events 基础规格不存在的归档提示未改，不是本轮新增错误。
- 本地构建有 vinext 既有路由静态分类提示；构建成功不等于线上发布。
- 截图为虚构测试内容；邀请字段遮盖。输出及日志保留在工作区 `outputs/friend-polish-qa`。
- 尚未验收：用户对新布局的集中预览、Android 微信实际触摸/听感、候选部署后的生产冒烟。自动化手机尺寸不替代真机。
