# Android 微信真机验收 · Refs #95

## 当前发布结论（用户最终补充）

用户明确确认：手机访问 `preview.thelivingwall.cn` 能点击“给朋友留一份心意”，此前关于手机入口不可点击的描述不适用于当前自定义域名候选版。结合下方用户已确认的声音、共同塑造、刷新与滚动验收，候选应用可进入发布检查。平台 `edgeone.dev` 在大陆网络的 401 是默认域名访问限制，不作为自定义域名发布阻塞。

保留“红框上方才是活动区域”为后续待核实体验问题，按用户要求本轮仅记录，没有追加改变画布或触摸算法。不得把用户反馈改写为该问题已修复。

本轮额外尝试了隔离桌面 Chrome 的 Android UA/触摸自动化：预览站、正式站各两个声音模式均在 `page.goto(... waitUntil: networkidle)` 等待阶段 25 秒超时，没有执行入口断言，不能计为点击失败或通过。用户澄清后不再重跑，采用其明确的手机实测结论；没有改写已有通过测试。最后应用候选仍为 `faa5cd0`，随后同步主线及验收文档不改变应用代码。发布前重新核对同步后的 CI；合并、生产部署及线上冒烟结果另行记录。

## 用户确认：复用已通过的人工验收

用户明确以下项目在此前测试中均已通过；相关代码未变化时，不再要求重复人工测试，可按改动范围补自动化验证：

1. 声音开启后的实际听感与提示一致；关闭后离开页面再返回，不自行播放。
2. 手机与电脑共同确认后刷新，留言及共同选择结果保留。
3. 向下滑动能完整查看“一起塑造小莹”的内容，不会一直被底栏挡住。

记录性质：用户对已有测试结果的明确确认，不是本轮新执行的自动化或人工操作；具体历史测试版本不补造。此前笼统的“待验收”清单以上述反馈为准。第 3 项不能被重新列为已确认的滚动遮挡缺陷；用户所报“只有红框上方是活动区域”仍单独保留，需区分活动范围与内容滚动可达性。未因该确认宣称已修复活动范围。

后续验证原则：只有相关代码变化或出现新证据时重验相应行为。布局修改优先复用 `scripts/friend-layout-browser-qa.mjs` 并针对画布尺寸、滚动、工具栏边界及触摸命中补验证；桌面自动化不能冒充 Android 微信真机结果。本轮仅更新记录，没有修改应用代码。

## 新反馈：手机页面活动区域受底部工具栏影响（待核实）

- 用户原话：“现在只有红框上面的位置是活动区域，先记录一下这个问题”。
- 场景：手机微信中的自定义预览域名朋友详情页。截图底部红框圈出固定工具栏，包含保存期限、保存说明、开启声音、声音设置和互动设置；用户反馈活动区域仅在该区域上方。
- 截图中小莹与留言文字存在视觉重叠，底部工具栏背景下隐约可见后续内容。是否存在触摸被覆盖、可视区域缩小、滚动不到内容末尾或小莹运动范围受限，尚未分别验证，不能仅凭静态截图判定根因。
- 既定要求：小莹不因文字布局被限制在右侧；手机内容与交互控件应可达。后续核对底栏占用、页面滚动、画布和触摸命中范围。
- 本地截图：`/var/folders/6g/f3ft9mrs3279wklcl6wzxrr80000gn/T/codex-clipboard-6dbfd589-85cc-4c20-90ca-fc901606231f.jpg`。不记录邀请凭证或私人留言正文。
- 截图已显示接收方详情与双方留言，表明本次页面进展至交流阶段；不据此将声音、共同确认、刷新或整轮真机验收标为通过。之前的创建失败及来源预检修复记录保留。
- 本轮按要求仅记录，未修改代码、云配置或重新部署。

## 来源授权修复后的复查

2026-09-26 用户截图确认，实际控制台入口为“HTTP 网关 → 跨域设置”，路由 `#/env/http-access/safety-domain`；列表已有精确域名 `preview.thelivingwall.cn`。此前指向安全管控、身份认证的菜单指引不准确，以此实际页面为准。

15:42:35（北京时间），对下方记录的相同匿名登录接口、Origin、预检方法及请求头重新执行一次 OPTIONS：由 403 变为 `204 No Content`，明确返回 `Access-Control-Allow-Origin: https://preview.thelivingwall.cn`。请求 ID：`cef20e9b-69a1-4a4b-9bb5-8ceac4228a89`。来源预检阻塞已解除；未登录、未创建或读取交流。请在原创建页面沿用保留的请求重试一次，实际创建及后续真机行为仍待验收。无需重新部署。

状态：自定义预览域名已能显示创建页；用户反馈生成分享链接重试约 5 次仍未成功，创建流程验收未通过，后续收发验收受阻。桌面触摸模拟结果不计入本记录。

## 创建阻塞：已确认来源预检拒绝

用户随后说明无法继续测试，开始限定诊断。2026-09-26 15:16:38（北京时间）对已部署 SDK 使用的匿名登录入口发出同参数 OPTIONS 请求，仅改变 Origin：

- 接口：`https://env-d1g2bv5sn355fc36e.api.tcloudbasegateway.com/auth/v1/signin/anonymously`。
- 预检参数：`Access-Control-Request-Method: POST`、`Access-Control-Request-Headers: content-type,x-sdk-version`。
- `Origin: https://preview.thelivingwall.cn` → `403 Forbidden`，无 `Access-Control-Allow-Origin`。平台请求 ID：`cf3a2af1-9e01-4ad7-bbbe-9ca1f528fcc8`。
- 对照 `Origin: https://thelivingwall.cn` → `204 No Content`，`Access-Control-Allow-Origin: https://thelivingwall.cn`。平台请求 ID：`5a6794d8-cd86-4ad3-a4dd-ac9924a03694`。

结论：新预览来源在 CloudBase 网关的来源检查处被拒绝，是明确的创建前置阻塞。该检查没有登录、创建心意或读取用户留言；不是用户此前五次请求的逐次服务端追踪，不能追溯认定此前是否存在落库记录，也不证明修复来源后所有业务行为已通过。

公开预览 HTML/JS 获取成功：`/friends/assets/index-oS1SGLQH.js`，加载 `cloudbase-client-BHm19Pkx.js`，公开配置为 `env-d1g2bv5sn355fc36e` / `ap-shanghai` / `xiaoying-gifts-sdk-probe`，符合候选配置。沙箱内初次 DNS 解析失败，经允许只读网络请求后获取成功，不记作站点 DNS 故障。未重试浏览器控制。

最小修复：在 CloudBase 该环境的“环境配置 → 安全来源 → 安全域名”核对并添加精确域名 `preview.thelivingwall.cn`。只放行该预览来源，保留既有函数、数据库和清理函数权限，不使用通配符。需用户确认具体来源授权影响后操作；目前尚未改变云配置。官方配置说明：https://docs.cloudbase.net/envconfig/security/intro 。生效后先重复同一 OPTIONS 检查，再在原页面进行一次创建重试和后续人工验证；无需为此重建前端。

## 最新反馈：生成分享链接连续重试未成功

- 测试入口：`https://preview.thelivingwall.cn/friends/`。
- 用户原话：“先记录问题，在生成连接中重试了好5次都没有成功”。记录为约 5 次重试，未逐次计数或测量耗时。
- 截图可见：创建表单仍在，按钮为“重试生成链接”，提示为“暂未收到保存确认，内容已保留。”；未出现生成成功的邀请链接。
- 结果：创建流程验收未通过；无法继续基于这次创建验证邀请、收发和共同确认。表单内容仍可见，但未验证刷新后的保留情况。
- 证据边界：这条反馈属于 Android 微信验收过程，但本张截图未包含浏览器外框，不能仅据此确认发生问题的具体设备与浏览器；当前部署 SHA 未重新核验。没有网络请求、平台错误码或服务端日志，不能认定是超时、安全域名、权限、网络或后端故障，也不能认定服务端一定未保存。
- 本地截图：`/var/folders/6g/f3ft9mrs3279wklcl6wzxrr80000gn/T/codex-clipboard-224b2cce-a67b-4a71-a47c-21e78d33f858.png`（未上传到公共仓库）。
- 用户要求先记录：本轮只更新验收记录，未修改应用代码、DNS、云权限或部署，未再次触发生成请求。

此前用户截图已显示 `preview.thelivingwall.cn` 关联预览环境、CNAME 生效，随后免费 HTTPS 证书申请中。本次截图证明创建页可显示；尚未单独核验证书最终部署状态。下方早期入口记录保留为过程信息，以本节状态为准。

## 401 诊断修正（后续核实）

用户按控制台“访问站点”操作仍得到同一裸域名。此前“重新生成三小时鉴权链接”的指引撤回：项目概览截图显示“全球可用区（不含中国大陆）”，官方文档明确该区域的项目域名及部署域名从中国大陆网络访问会返回 401；三小时鉴权规则适用于另外两个加速区域。当前现象与区域限制一致，尚未单独测量手机网络出口。来源：https://edgeone.cloud.tencent.com/pages/document/175191784523485184 。下方早先指引保留为过程记录，以本节修正为准。

下一步建议检查现有域名的独立子域名并绑定预览环境，不改变正式域名指向、不购买新域名、不调整加速区域。平台支持预览环境自定义域名，但默认指向该环境最新成功部署，测试前应复核提交。来源：https://pages.edgeone.ai/document/custom-domain 。如 CloudBase 需要新增安全域名，应单独核对具体范围再处理。尚未修改域名、DNS 或云权限。

## 测试入口

候选分支 `livehighhigh/95-friends-polish`，应用候选提交 `faa5cd0e34a4787511f214d87ba314dd27202caf`，PR #98。

使用现有 EdgeOne 项目的独立 HTTPS 预览部署；核对部署分支、提交和预览环境后才开始。正式域名目前不作为本次修复的验收入口，127.0.0.1 也不能从另一台手机访问。

2026-09-26：控制台可读取并打开“新建部署”；选择分支时浏览器操作超时，恢复读取也超时。没有提交部署，未改变云权限或环境变量。请用户在弹窗中选择上述候选分支，先核对环境为预览。未取得预览 URL，不能记为部署成功。

随后用户提供控制台成功截图：部署 `dpnmaptpqv8c`，预览环境，分支 `livehighhigh/95-friends-polish`，提交 `faa5cd0`，时间 2026-09-26 14:17:25，构建用时 60 秒。测试入口：https://the-living-wall-dpnmaptpqv8c.edgeone.dev/ 。这是部署结果证据，不是真机行为通过证据。

预览域名若被 CloudBase 安全域名校验拒绝，单独记录具体错误；不要据此扩大通用权限或声称业务验证通过。

## 一轮操作（约 5–10 分钟，不含部署等待）

记录手机型号、Android 版本、微信版本、预览部署 ID / SHA / URL、测试时间。只使用虚构称呼和留言，不提交邀请令牌或私人聊天。

1. 在 Android 微信内打开候选首页。点击“开启声音”，触摸小莹，确认实际听到声音，按钮和提示准确一致；点击“关闭声音”，切到微信聊天再返回，确认不会擅自恢复播放。
2. 点击“给朋友留一份心意”，应能进入创建页。填写虚构留言，弹出/收起软键盘，检查输入和提交按钮可达，底栏不遮挡表单。
3. 创建测试心意，复制邀请链接；在电脑独立浏览器打开并接受，互发一条测试留言。复制失败时检查可选择完整链接的回退入口。
4. 手机向下滚动、展开历史或玩法说明，检查声音和“互动设置”可达，内容末尾不被底栏遮住；打开保存说明再关闭，页面仍能正常操作。
5. 双方共同选择一个样子，手机刷新，确认留言和共同结果仍在。切横屏再返回竖屏，检查底栏、说明弹窗和左右布局没有溢出或遮挡。

## 结果

- 设备 / 系统 / 微信：待用户提供
- 预览部署 / SHA / URL：`dpnmaptpqv8c` / `faa5cd0` / https://the-living-wall-dpnmaptpqv8c.edgeone.dev/ （用户控制台截图）
- 入口点击：待验收
- 声音真实听感、提示、关闭后返回：待验收
- 软键盘、底栏、设置、保存说明：待验收
- 分享链接生成：约 5 次重试仍未成功，验收未通过；后续收发、共同确认与刷新：受本次创建问题阻塞
- 横竖屏：待验收
- 缺陷与截图：见上方“最新反馈：生成分享链接连续重试未成功”

本轮成功后再决定合并及生产冒烟；不要求重做先前已通过的全部生产测试。

## 真机入口阻塞：EdgeOne 401

用户将裸预览域名发送至文件传输助手后，在 Android 微信内打开，截图显示 Tencent Edgeone `401: UNAUTHORIZED` / `Access Restricted or Authentication Expired`。这证明请求被平台访问层拦截，尚不能归因于微信兼容性或 CloudBase SDK。此前助手提供裸域名作为测试入口，未验证跨设备预览鉴权，指引不充分。

下一步从同一部署的控制台“预览”入口生成有效链接，保留完整鉴权参数再在手机打开；不重新部署、不关闭访问保护、不扩大云权限。官方部署说明注明预览链接有效期三小时：https://pages.edgeone.ai/document/deployment-overview 。若完整新链接仍返回 401，再核对加速区域及网络限制：https://pages.edgeone.ai/document/error-codes 。临时鉴权参数不入库。
