# 赠光原型：可用性修正

Refs #69；对应 PRD UI-01/02/03。这是独立体验原型，不是线上应用路由，不调用 API，不读写个人成长档案，不含真正分享与跨设备回声。新增陪伴入口、创作、接收三个视角；接收无需确认收下。

复用 `lib/creature.ts` 和 `lib/draw-creature.ts`，固定演示成长参数。状态文字读取引擎 phase/resting；暂停不推进状态。留言取消丢弃未保存编辑；保存后显示编辑入口。默认示例留言跟随心意变化，已保存留言不被切换覆盖。

## 构建

在仓库根目录安装锁文件依赖后执行：

```sh
node prototypes/quiet-gift/build.mjs /tmp/xiaoying-quiet-gift.html
```

构建使用锁文件中已有的 esbuild。输出为对话预览片段；若需要普通浏览器预览，可用当前可用的 visualize 渲染工具包装。片段依赖预览宿主的 Lucide 图标，普通浏览器独立嵌入时需补图标环境。正式应用集成与发布另行处理。

## 本轮验证

- 已在本机独立 Chrome 浏览器中检查：取消保留原文、保存后可编辑、已保存留言不因心意切换被覆盖、回声提交文案、暂停提示、360px 窄屏无横向溢出、无脚本错误。
- 默认 Playwright 浏览器未安装；改用已有 Chrome。沙箱首次启动受限，经正常审批后运行成功。
- 以上不代表手机真机或用户审美验收通过，也不代表真实发送/回声服务完成。

## 声音试听边界

构建时内嵌现有 touch.mp3 与 voice.mp3（两段约 48KB），保留页面内来源和许可；没有运行时网络请求。仅复用两种回应及 SoundDirector 的触发判断，不是完整网站音频引擎。主动开启先播放轻触样音，后续按互动触发。切换视角、暂停、切后台即停止且需重新开启；开关不进入共享状态。完整声音编排、手机听感与用户审美待验收。
