# The Living Wall · 小莹 协作约定

人和 AI 共用的开发流程。产品定位、模块与功能编号以 [`docs/PRD.md`](./docs/PRD.md) 为准；本文件不重复产品细节。根目录 `README.md` 是开发者快速说明。

## 事实来源

- 已实现行为与边界以代码、`docs/PRD.md`、`docs/03-呼吸与陪伴.md` 和自动化测试为准。
- 讨论稿、报名材料与未合并 PR 说明方向，不得写成线上已完成能力。
- 合并会改变用户可见行为或业务规则的 PR，须在同一 PR 内更新 `docs/PRD.md`（或写明「无行为变化，文档未改」）。
- 屏幕重合、鼠标抚摸不等于真实墙面接触；未经验证不得声称深度接触、毫米级精度或现场投影已完成。

## 开发流程

统一采用：**查看已有 Issue → 新建或认领 Issue → 同步代码 → 创建分支 → 开发与验证 → PR → 合并 → 按需发布**。

1. **Issue 先行**：新功能、缺陷修复、重构、行为参数调整及实质性文档变更，开发前必须关联 Issue。纯错字、链接修正可只提 PR。
2. **查重与写法**：创建前先搜索已有 Issue。写明目标、范围、验收条件、负责人、涉及模块和依赖；产品任务关联 PRD 功能编号。
3. **一对一**：默认一个可独立验收的 Issue 对应一个分支和一个打向 `main` 的 PR；大需求先拆成子任务 Issue。
4. **同步**：开工前检查工作区并保留本地改动，获取远程更新。新任务从最新 `main` 创建分支；续做任务同步其分支并检查 `main` 变化。**PR 的 base 默认是 `main`。** 改同一批文件的功能默认排队合入，不叠分支。
5. **分支命名**：分支名带 Issue 编号，前缀为创建者的 GitHub 用户名：`<github用户名>/<编号>-<简短名称>`（例如 `livehighhigh/1-product-docs`）。人和 AI 都用这一规则，不用 `codex/`。
6. **协调**：修改重叠模块前在 Issue 中协调；任务变化、阻塞和交接也记录在 Issue 中。
7. **叠 PR 是破例**：仅当上层硬依赖下层尚未合入的提交时才允许。Issue 写明依赖（如 `depends on #N`）；PR 正文写清底座分支，以及下层合入后谁负责把 base 改回 `main`。对 GitHub 默认只保留一张打向 `main` 的 PR。本地可以先叠着改；不要让上层 PR 长期指着下层功能分支。
8. **先改 base，再删分支**：合并前检查是否有未关闭 PR 把本分支当 base。有则先把那些 PR 改打 `main`（必要时 rebase），再合并、再删分支。合入后不要立刻删除仍被上层 PR 当作底座的分支。底座被删导致无法 reopen 时：rebase 到新 `main` 另开 PR 并关闭旧单；一个 Issue 最终只对应一张会进 `main` 的 PR。
9. **轻量分支与隔离**：采用 `main + 短期任务分支`，保持 `main` 可验证、可发布；没有版本冻结或多版本维护需求，不增加长期 `develop/release` 分支。尽早开 Draft PR；任务过大时拆分可独立验收的 Issue。并行任务使用独立 worktree 或克隆目录，不在同一工作目录同时切分支、提交或改写历史。
10. **历史修改**：共享分支通过 merge 同步，禁止强推 `main` 或擅自改写共享历史。个人分支确需 rebase 时，先核对远端提交、协作者及依赖 PR；更新远端仅用带明确预期提交的 `--force-with-lease=<ref>:<expected-sha>`，禁止裸 `--force`。

## OpenSpec：由 AI 按任务选择

- **默认轻量**：默认 Issue＋PR。AI 按当前任务判断，不让用户先分类或学习命令；项目大、文件多、代码量大或单纯有人接手，不是启用理由。
- **启用条件**：涉及行为或数据规则变化、存在影响实现的重要歧义，或多个协作者需要共同约定接口与验收标准时，AI 应使用 OpenSpec。明确的小修复不创建变更；执行中出现上述情况再补规格。例：修正文案用 Issue＋PR；调整成长奖励、每日上限和旧存档兼容用 OpenSpec。
- **操作与准备**：启用时先检查仓库 OpenSpec 配置、固定版本依赖和项目 skills，按现有提案、实施、归档流程执行；工具缺失时明确说明，先在 Issue 记录约定，不声称 OpenSpec 已启用，不自动安装其他工程工具。AI 维护文件和执行命令，成员负责需求、取舍与结果验收。
- **过程可见**：开工时用一句话说明选择理由。启用 OpenSpec 后，实施前展示简短中文说明（为什么改、范围、关键行为、验收结果），附实际文件链接；需求明确可直接推进，存在产品取舍时询问具体业务问题，不把“是否使用 OpenSpec”交给用户审批。团队阅读的提案、规格与设计说明优先中文，保留工具要求的结构标记。
- **少写、同步**：Issue 管责任与进度，PRD 管产品状态，OpenSpec 管本次变更约定，PR 管差异与验证证据；用链接关联，避免重复维护全文。设计只记录影响理解的决策和取舍，不堆叠调试日志。范围或关键决策变化时展示变化并同步相关约定。
- **结果与成本**：交付时对照约定展示证据、限制和未完成项。验证方法失败时先短诊断，避免无依据反复重跑；需要替代验收时，由人确认适用范围并记录原因与证据，不改写失败记录，不将个案放宽推广为通用规则。格式校验通过不等于行为正确；测试不能替代适用的人工体验验收。

## 评测、合并与发布

- PR 使用仓库模板，关联 Issue，说明实现内容、测试证据及部署影响；验收完整完成后才关闭 Issue。仍需部署、现场验收或管理员操作的 Issue 使用 `Refs #N`，不得用 `Closes/Fixes #N` 在合并时提前关闭。
- **AI 负责实现、自检与验证证据；人类负责结果验收、范围决策和进度管理。** 常规代码、文档、CI 和可回退的发布流程改进，在检查通过、合并所需验收条件满足后可自行合并，无须另一人逐行复核。涉及权限扩大、生产数据删除或迁移、不可逆操作、费用或对外承诺的变更，须由人类确认具体影响；用户可指定某项变更必须人工复核。
- 合并前再次同步并检查差异；冲突解决后重新验证。不直接推送 `main`，不擅自重写共享历史。若有未关闭 PR 依赖本分支，先改它们的 base，再勾选「删除分支」。
- 修改应用代码时，按影响执行 `npm test`、`npm run typecheck`、`npm run lint` 与 `npm run build`；视觉变化补充浏览器操作验证。
- **合并门禁**：拟合并的最新改动必须通过 CI（test、typecheck、lint、build），未解决的审查意见须处理。追加提交、解决冲突或改变 base 后重新确认检查和验收；`main` 前进后同步并重跑。平台保护配置见 [`docs/github-governance.md`](docs/github-governance.md)；配置文件入库不代表保护已启用，本地钩子不能替代服务器门禁。
- **合并方式**：普通独立任务默认 squash；确需保留提交历史或有堆叠依赖时，在 PR 中说明合并方式与后续分支处理。合并后确认无依赖 PR、未合入工作或其他工作区使用，再清理任务分支，不复用已完成分支。
- **发布边界**：任意分支可发布预览；生产默认发布已合入 `main` 且验证通过的确定提交。例外须在 Issue/PR 记录原因、负责人、验证证据与回合主线计划；涉及上述需人类确认的影响时，先取得确认。按目标环境验证实际构建产物；记录环境、提交 SHA、操作者、验证结果与回退版本，同一环境串行发布。线上版本以部署记录为准，**合并完成不等于发布完成**。
- **紧急修复与回退**：若 `main` 含未发布内容，可从实际生产提交派生修复分支，仍关联 Issue、走 PR 和必要检查；上线后将修复回合 `main` 并验证。代码撤销通过 revert PR，不重置主分支历史；部署回退单独记录，不等于代码已撤销。

## 验证诚实性

- 在 PR 中说明实际运行过的检查；不得把未运行的命令描述为已通过。
- 现有行为测试通过不能替代浏览器体验验收；物理触摸与投影验收不能用屏幕原型结果代替。

## 安全与数据

- 不得提交密钥、令牌、可识别个人身份的数据或生产凭据。
- 成长档案仅存于用户本机浏览器；清理站点数据会丢失记录。不得擅自引入云端同步或身份识别。
