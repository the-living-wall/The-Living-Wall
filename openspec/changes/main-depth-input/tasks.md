# Tasks

实施准备记录（2026-09-19）：已创建 [Issue #50](https://github.com/the-living-wall/The-Living-Wall/issues/50)，从最新 main（82c499b）克隆到 102 目录下的独立工作副本，并创建 `livehighhigh/50-main-depth-input` 分支；原下载副本与 102 根目录的未提交改动保留。

## 1. 开发准备

- [x] 1.1 查重并关联独立Issue，说明依赖#25及本地主前端范围；核对代码来源，保留下载副本，创建符合AGENTS约定的任务分支；以Issue链接和git状态验证。

## 2. 实现

- [x] 2.1 增加只读本地开发桥接，验证同源正常访问、非本地主机/跨域/未知路径拒绝、上游失败与超时有明确信息，生产无该接口。
- [x] 2.2 实现最大近墙区域适配，测试真实与模拟模式、无效和过期数据、非法坐标、最大区域选择及镜像。
- [x] 2.3 主页面接入互斥深度模式、原版渲染与状态提示；浏览器验证开启、退出、校准中、无区域及失联清除，不自动启用RGB。
- [x] 2.4 隔离实验存档；测试深度实验不改变普通成长，退出后恢复普通存档。
- [x] 2.5 更新PRD IN-03/HW-01及本地操作说明，明确近墙实验、非手部识别、投影映射待验收；检查文档与实际入口一致。

## 3. 验证与交付

- [x] 3.4 同伴可复现交付：纳入现场服务源码及测试，说明普通前端/相机两种启动路径；从干净代码副本安装依赖并验证，不依赖原机器 .runtime。

可复现验证补充（2026-09-20）：从 Git 暂存索引导出干净副本，新建 Python 3.12 venv，`npm ci` 与 `pip install -r requirements.txt` 成功；40 项前端测试、typecheck、lint、build、OpenSpec strict validate、24 项 Python（含服务→前端适配协议）和 3 项 guide 测试通过。独立端口前端主页及深度 `/api/state` 均返回 200，未打开相机或中断原现场服务。初次 lint 发现上游 guide 测试未 await，补齐后重跑通过。NumPy 矩阵运算仍有数值警告；npm 安装报告现有 11 项漏洞（1 low / 2 moderate / 8 high），未执行强制升级。此次验证限本机 Apple Silicon macOS，不代表 Windows/Linux 相机或完整现场验收通过；3.2 仍未完成。此前 `.runtime` 依赖缺口已补齐，核心服务与来源提交 bd167a1 内容一致。

- [x] 3.1 运行npm test、typecheck、lint、build与新适配测试，记录真实结果，生产构建检查实验入口隔离。
- [ ] 3.2 浏览器验证原版画面、输入位置、镜像、断流和模式切换；真实相机可达与近墙移动由现场配合确认，分别记录自动验证和人工体验。
- [x] 3.3 PR关联Issue并说明未完成的现场验收；提供主前端测试入口，不将本地完成描述为生产发布。

验证记录（2026-09-19，分支 `livehighhigh/50-main-depth-input`）：
- `npm test`：40/40 通过；`npm run typecheck`、`npm run lint`、`npm run build` 通过；OpenSpec strict validate 通过。深度工具原有 Python 几何测试 9/9 通过，坏校准测试出现预期数值警告。
- 开发服务 `http://localhost:3018/`：桥接从 8769 读取状态返回 200，移除 `image`；跨域 403、未知路径 404。相机服务未启动时返回明确的 502。
- Chrome 浏览器：原版画面、开启/退出、空墙、校准中、有效区域、双向镜像、区域消失与断流提示均已操作验证。合成坐标 [0.2,0.3] 在双向镜像下显示为 80%/70%，关闭镜像后为 20%/30%；区域消失后标记被清除。
- 生产构建本地预览主页 200，`/__depth-lab/state` 返回 404，页面 HTML 不含深度实验入口。尚无真实相机现场数据；不能把合成状态验证写成真实手部或物理触碰验收。
- 已创建 [Draft PR #51](https://github.com/the-living-wall/The-Living-Wall/pull/51)，base 为 `main`，关联 Issue #50 并列明现场验收未完成；本地开发测试入口为 `http://localhost:3018/`，需要同时启动 #25 的相机服务。真实相机验收前保持 Draft，不合并、不发布。

后续现场补充（2026-09-19）：真实 Gemini 335 已通过 pipe 模式接入并校准；60 秒动作观察获得 237 次轮询，其中 139 次有效近墙输入、98 次等待，无无效状态或请求错误，末尾区域归零。用户确认碎光有反应、方向大致一致，但体感慢且有效范围不清楚。详见[实测记录与待办](../../../docs/evidence/depth-input/2026-09-19-live-test.md)及其 JSON 摘要。此前“尚无真实相机现场数据”是早期验证状态，以本补充为最新进度。3.2 暂不勾选：现场镜像/断流/模式切换等仍未完整验收，且本次依赖 `.runtime/depth-lab-v2` 的本地服务副本，PR 内旧服务的可复现性问题尚待解决。
